package com.example.aries;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

class AriesApplicationTests {

    @Disabled("Requires running MongoDB and ANTHROPIC_API_KEY — run manually against a live environment")
    @Test
    void contextLoads() {
    }
}
